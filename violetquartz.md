# ResourceHarbor

ResourceHarbor 是一个专注于互联网技术资源与优质外链的聚合与导航平台，面向开发者、技术研究者以及信息检索人员，提供结构化、可溯源的外部资源索引能力。该项目通过人工筛选与社区贡献相结合的方式，收集并分类整理各类技术文档、工具站点、学术资源及行业动态来源，帮助用户在不依赖搜索引擎竞价排名的情况下，快速定位高价值外部信息。ResourceHarbor 本身不存储或托管任何第三方内容，仅提供元数据描述与链接跳转，致力于构建开放、中立、可持续的网络资源基础设施。

## 功能概览

- **多维度资源分类体系**：按技术领域、资源类型、适用人群、内容形式等维度对收录链接进行精细化标签管理，支持快速筛选与批量导出。
- **社区驱动的链接提交与审核**：注册用户可提交未收录的外部链接，经由维护者及核心贡献者双重审核后纳入索引库，审核记录完全公开可追溯。
- **链接健康状态主动监测**：内置周期性可用性检测机制，对已收录链接进行 HTTP 状态码与响应时间监控，自动标记失效或迁移节点，保障索引有效性。
- **结构化元数据提取与展示**：对每个收录链接自动抓取页面标题、描述、关键词及语言信息，并支持手动补充说明字段，便于用户评估内容相关性。
- **个人收藏与自定义集合**：支持用户将常用链接归入个人收藏夹，并可创建命名集合（如"机器学习数据集源""CI/CD 工具链"）进行分组管理，集合支持共享链接。
- **全文检索与高级过滤**：基于标题、描述、标签、域名、收录时间等字段提供组合查询能力，支持正则表达式过滤与结果排序（按相关性、新增时间、稳定性等）。
- **开放 API 接口**：提供 RESTful API 供第三方开发者批量获取资源列表、检索单条链接信息或订阅更新动态，接口响应格式支持 JSON 与 RSS。

## 应用场景

- **技术选型调研**：架构师或技术负责人可通过 ResourceHarbor 快速检索特定领域（如消息队列、ORM 框架、监控系统）的官方文档、社区讨论帖、性能对比报告等链接集合，大幅减少重复搜索时间，提高调研效率与信息可信度。
- **学术研究参考文献收集**：高校师生或科研人员在撰写论文或技术报告时，可利用本项目的分类标签和元数据筛选功能，集中查找与研究方向相关的预印本服务器、数据集发布页、开源项目仓库及行业标准规范站点，形成规范的外链引用清单。
- **自动化运维脚本依赖源管理**：DevOps 工程师在编写部署脚本或 Dockerfile 时，常常需要引用外部软件包镜像源、补丁下载地址或配置模板库。通过 ResourceHarbor 的健康监测功能，可优先选择稳定性较高的源地址，并定期导出可用链接列表供脚本动态拉取。
- **内部知识库外链治理**：企业或组织在建设内部 Wiki 或知识管理系统时，可使用 ResourceHarbor 作为外部链接的预审通道，由团队维护统一的外链白名单，避免内部文档中出现大量过期或恶意跳转链接，降低安全风险与维护成本。

## 快速开始

以下步骤将帮助您在本地环境中快速启动 ResourceHarbor 开发实例，用于测试、二次开发或私有化部署。

```bash
# 1. 克隆项目代码仓库
git clone https://github.com/resourceharbor/resourceharbor.git
cd resourceharbor

# 2. 安装项目依赖（使用 pip 与 npm）
pip install -r requirements.txt
npm install --prefix frontend

# 3. 初始化本地配置文件与数据库
cp .env.example .env
python scripts/init_db.py

# 4. 启动后端服务（开发模式）
python app.py --host 127.0.0.1 --port 8080

# 5. 启动前端开发服务器（可选，需另开终端）
npm run dev --prefix frontend
```

访问 `http://127.0.0.1:8080` 即可进入本地运行实例。默认管理员账户为 `admin@resourceharbor.local`，初始密码见 `.env` 文件中的 `DEFAULT_ADMIN_PASSWORD` 字段。

## 安装要求

ResourceHarbor 采用前后端分离架构，后端基于 Python 3.10 及 Flask 框架，前端基于 Node.js 18 及 Vue 3。生产环境推荐使用 PostgreSQL 作为主数据库，Redis 作为缓存与任务队列后端。以下为完整的安装依赖清单：

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.10.x 或 3.11.x | 后端运行环境，低于 3.10 将无法兼容异步语法及类型注解 |
| Node.js | 18.x LTS 或 20.x LTS | 前端构建与运行环境，用于编译 Vue 组件及管理 npm 包 |
| PostgreSQL | 14.x 及以上 | 主数据库，存储用户、资源元数据、标签、收藏关系等结构化数据 |
| Redis | 6.x 及以上 | 用于会话缓存、链接健康检测任务队列、API 限流计数器 |
| Nginx | 1.22 及以上 | 生产环境推荐作为反向代理服务器，处理静态资源缓存与负载均衡 |
| RabbitMQ | 3.10 及以上 | 可选组件，用于分布式链接检测任务的消息传递，单机部署可使用 Redis 替代 |

## 文档导航

ResourceHarbor 提供分层级的技术文档，覆盖从入门部署到核心架构设计的全部内容。下表列出主要文档目录及其适用场景：

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户指南 | `docs/user/quickstart.md` `docs/user/faq.md` | 如何使用资源检索、收藏、提交新链接？个人收藏集合如何共享？ |
| 运维手册 | `docs/ops/deployment.md` `docs/ops/monitoring.md` | 生产环境如何部署？如何配置健康检测周期？日志与监控如何接入？ |
| 开发文档 | `docs/dev/api_reference.md` `docs/dev/contribution.md` | API 接口的请求与响应格式是什么？新增分类标签的流程是什么？ |
| 架构设计 | `docs/architecture/database_schema.md` `docs/architecture/task_queue.md` | 数据库表关系如何设计？链接检测任务队列如何工作？如何扩展新的检测策略？ |

## 资源列表

本项目作为技术资源外链汇总平台，收录了来自社区贡献者提交的多个外部站点。以下列表按类别分组展示所有已收录链接，每个 URL 均保留原始输入格式，未做任何协议补全或域名规范化处理。所有链接的可用性与内容合规性由社区共同维护，欢迎提交反馈。

技术参考与开发社区

<code>qingqingcaochengrenwang.org.cn</code>

多媒体资源与示例素材

<code>oumeilingleisetu.org.cn</code>

导航与工具集合

<code>laosijiwangzhi.org.cn</code>

前沿技术讨论与资讯

<code>oumeibiantailinglei.org.cn</code>

学术与数据集索引

<code>yazhoubiantailinglei.org.cn</code>

行业标准与协议文档

<code>sishilurenqi.org.cn</code>

镜像站与代码托管加速

<code>yazhouzipaisetu.org.cn</code>

## 项目结构

ResourceHarbor 项目根目录采用模块化组织方式，各子目录职责清晰，便于维护与扩展。以下为项目核心目录树结构及对应说明：

```
resourceharbor/
├── app/                                # 后端应用主目录
│   ├── api/                            # RESTful API 路由与视图函数 (v1 版本)
│   │   ├── resources.py                # 资源增删改查、检索、导出接口
│   │   ├── collections.py              # 用户收藏与自定义集合管理接口
│   │   ├── tags.py                     # 标签体系管理接口
│   │   └── health.py                   # 链接健康状态查询与手动触发检测接口
│   ├── core/                           # 核心业务逻辑层
│   │   ├── crawler/                    # 元数据抓取与解析模块 (支持重试与超时控制)
│   │   ├── checker/                    # 链接可用性检测引擎 (支持 HTTP/HTTPS/TCP 多种策略)
│   │   └── indexer/                    # 全文索引构建与查询处理器 (基于 Whoosh)
│   ├── models/                         # 数据库 ORM 模型定义 (SQLAlchemy)
│   │   ├── user.py                     # 用户、角色、权限模型
│   │   ├── link.py                     # 资源链接、元数据、标签关联模型
│   │   └── task.py                     # 检测任务记录与队列状态模型
│   ├── services/                       # 外部服务集成层 (邮件、消息队列、缓存)
│   │   ├── redis_client.py             # Redis 连接池与缓存操作封装
│   │   └── mq_publisher.py             # RabbitMQ 任务发布器 (可选)
│   └── utils/                          # 通用工具函数库
│       ├── validators.py               # URL 规范化与安全性校验
│       └── formatters.py               # 日期、大小写、标签字符串格式化
├── frontend/                           # 前端单页应用目录 (Vue 3 + Vite)
│   ├── src/
│   │   ├── components/                 # 可复用 UI 组件 (导航栏、资源卡片、筛选面板)
│   │   ├── views/                      # 页面级组件 (首页、资源详情、收藏管理、提交表单)
│   │   ├── stores/                     # Pinia 状态管理 (用户会话、检索条件、收藏列表)
│   │   └── router/                     # Vue Router 路由配置 (含权限守卫)
│   └── public/                         # 静态资源 (favicon、默认占位图)
├── scripts/                            # 运维与开发辅助脚本
│   ├── init_db.py                      # 初始化数据库表结构及默认数据
│   ├── seed_links.py                   # 从 CSV/JSON 批量导入初始链接数据
│   └── health_scan.py                  # 独立运行的全量链接健康扫描脚本 (可加入 crontab)
├── tests/                              # 单元测试与集成测试目录
│   ├── unit/                           # 后端核心模块单元测试 (pytest)
│   └── e2e/                            # 前端端到端测试 (Playwright)
├── docs/                               # 完整项目文档 (Markdown 格式)
├── requirements.txt                    # Python 后端依赖清单
├── package.json                        # Node.js 前端依赖清单
├── docker-compose.yml                  # 开发环境容器编排 (PostgreSQL + Redis + 后端 + 前端)
├── .env.example                        # 环境变量配置模板 (含数据库连接串、密钥、检测周期)
└── LICENSE                             # MIT 开源许可证文件
```

## 贡献指南

ResourceHarbor 遵循开源社区协作规范，欢迎任何形式的贡献，包括但不限于提交新资源链接、修复文档错误、优化前端交互、完善后端检测逻辑等。请按照以下步骤参与项目贡献：

1.  **问题沟通与任务认领**：在 GitHub Issues 页面查找标记为 `good first issue` 或 `help wanted` 的工单，或自行创建新 Issue 描述您发现的问题或建议的新功能，等待维护者确认并分配标签。对于已有工单，请在评论区留言表明认领意向，避免重复工作。

2.  **派生仓库并创建特性分支**：将主仓库 Fork 至个人账户，然后克隆派生仓库到本地。基于 `main` 分支创建新的特性分支，分支命名规范为 `feature/简述改动` 或 `fix/简述修复`，例如 `feature/add-link-tag-filter`。请确保分支名称清晰反映改动内容。

3.  **编码与本地自测**：在本地环境完成代码或文档修改后，务必运行单元测试（后端执行 `pytest tests/unit`，前端执行 `npm run test:unit`）以及代码风格检查（`flake8` 和 `eslint`）。新增功能需同步编写对应的测试用例，确保代码覆盖率达到 80% 以上。

4.  **提交变更并推送**：编写符合规范（首行概括改动，正文说明动机与实现方式）的 Git 提交信息，然后推送到个人派生仓库的对应分支。提交前请确保已 rebase 主仓库最新的 `main` 分支，避免合并冲突。

5.  **发起合并请求**：在 GitHub 上向主仓库的 `main` 分支发起 Pull Request，并填写 PR 模板中的检查清单（如是否通过测试、是否更新文档等）。维护者将在 48 小时内进行代码审查，必要时会提出修改意见。所有 PR 必须经过至少一名核心维护者的批准方可合并。

## 常见问题

**问：ResourceHarbor 是否会对收录的外部链接进行内容审查或责任担保？**

答：ResourceHarbor 仅提供链接的元数据索引和可访问性检测，不审查、不修改、不托管任何第三方网站的实际内容。所有收录链接均来源于社区提交，项目维护者仅对链接的可用性（即 HTTP 状态码）进行技术性验证，不对目标站点的内容合法性、准确性或安全性做任何明示或暗示的担保。用户在访问外部链接时，应自行遵守相关法律法规并评估访问风险。

**问：如何报告一个已收录链接的失效或跳转异常情况？**

答：您可以通过两种方式报告链接异常。第一种是在项目仓库的 Issues 中提交工单，标题注明 `[Link Broken]` 并附上具体 URL 和异常现象（如返回 404、超时、重定向循环等）。第二种是直接在前端页面点击该链接条目旁的"报告问题"按钮，系统将自动生成包含当前检测状态的工单。维护者会在收到报告后的 2 个工作日内进行人工复核并更新链接状态。

**问：私有化部署时，是否可以完全脱离外部网络运行？**

答：可以。ResourceHarbor 的核心功能（资源检索、收藏、标签管理、本地 API 调用）完全依赖本地数据库和缓存服务，无需访问外部网络即可正常运行。但请注意，链接健康监测功能默认会向目标站点发起 HTTP 请求，这属于正常出站流量；若您的部署环境对出站流量有严格限制，可在配置文件中将 `CHECKER_ENABLED` 设置为 `false` 以禁用该功能，仅保留手动检测入口。元数据自动抓取功能同理，可通过配置关闭。

## 许可证

ResourceHarbor 使用 MIT 开源许可证。该许可证允许任何个人或组织免费使用、复制、修改、合并、发布、分发、再许可及销售本软件的副本，但需在软件的所有副本或实质性部分中保留原始版权声明和许可声明。本软件按"原样"提供，不提供任何形式的明示或暗示担保，包括但不限于适销性、特定用途适用性和非侵权性的担保。有关完整许可条款，请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 7 | 生成时间: 2026-08-02 21:19:29
