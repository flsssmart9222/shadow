# Navigator Core

Navigator Core 是一个面向技术社区与数据驱动型站点的轻量级导航与资源聚合框架。项目定位于为开发者、运维人员及数据分析师提供可快速部署的外部信息源统一管理入口，通过结构化索引与标准化外链组织方式，解决多源数据站点分散、检索效率低、资源可信度难以评估等问题。该项目本身不存储任何业务数据，仅作为外部资源的逻辑映射层，适用于个人工具集构建、团队知识库外链治理、以及自动化数据采集管道的前端引导场景。

## 功能概览

- **集中化外链管理**：支持将多个外部数据站点按领域、标签或使用频率进行分组，并提供只读式索引视图，便于快速定位目标资源。

- **可插拔的解析适配器**：内置基础 HTTP 请求头伪装与超时控制逻辑，可针对不同域名配置独立的抓取间隔与重试策略，降低被目标站点限流的风险。

- **静态化站点生成**：提供基于模板引擎的构建流程，可将外链配置与元数据描述编译为纯静态 HTML 页面，无需后端服务即可部署至任意 Web 容器或对象存储。

- **健康状态探测**：周期性对已收录域名执行 TCP 连接检测与 HTTP 状态码验证，异常结果通过日志文件或可选的 Webhook 渠道输出，辅助管理员及时发现资源失效情况。

- **访问统计看板**：集成轻量级计数接口，记录各外链的点击频次与来源 IP 地域分布（不涉及用户身份识别），数据以 JSON 格式本地化存储，便于后续分析。

- **权限分级视图**：支持基于 IP 网段或简单令牌（Token）的访问控制，可将部分敏感外链（如内部测试环境或付费数据源）隐藏于公共视图之外，仅对授权网段可见。

- **配置热加载**：外链配置文件采用 YAML 格式，修改后无需重启服务进程，通过文件系统监控机制实现动态生效，提升运维灵活性。

- **导出与迁移工具**：提供命令行接口，可将当前外链配置导出为 CSV 或 Markdown 表格，方便团队内部评审或迁移至其他导航系统。

## 应用场景

- **个人开发者的日常数据源聚合**：开发者可将常用的比分数据、实时排名、行业资讯等外部站点统一收录至 Navigator Core，通过本地启动的导航页实现一键跳转，替代浏览器收藏夹的杂乱管理方式。

- **小型数据团队的协作入口标准化**：团队内部不同成员负责不同的数据采集任务时，可通过 Navigator Core 建立共享的资源清单，配合健康探测功能定期检查各数据源的可用性，减少因外链失效导致的采集任务中断。

- **技术博客或开源项目文档的延伸链接区**：项目维护者可在文档站点中嵌入由 Navigator Core 生成的资源导航页面，为访客提供额外的参考资料或数据验证来源，增强文档的实用性与权威性。

- **自动化脚本的源地址配置中心**：运维人员可将 Navigator Core 作为配置管理的前置层，脚本通过 API 接口获取最新的外链列表，避免在代码中硬编码域名，降低变更时的维护成本。

## 快速开始

以下步骤适用于 Linux 与 macOS 环境，Windows 用户建议通过 WSL2 或 Git Bash 执行。

```bash
# 步骤 1：克隆项目仓库
git clone https://github.com/navigator-core/navigator-core.git
cd navigator-core

# 步骤 2：安装项目依赖（需要 Go 1.21+ 或 Docker）
make install-deps
# 若使用 Docker，可执行：docker build -t navigator-core .

# 步骤 3：启动本地服务（默认端口 8080）
./bin/navigator-core --config ./configs/example.yaml --port 8080
# 或使用 Docker：docker run -p 8080:8080 navigator-core
```

启动成功后，访问 `http://localhost:8080` 即可查看默认示例页面。如需自定义外链列表，请编辑 `configs/example.yaml` 文件，并参考 `docs/configuration.md` 中的参数说明。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Go 编译器 | 1.21 或更高 | 用于从源码编译构建，若使用预编译二进制包则可跳过 |
| Docker Engine | 20.10 或更高 | 可选，用于容器化运行，推荐生产环境部署 |
| GNU Make | 3.81 或更高 | 用于执行安装脚本与构建任务，Linux 通常预装 |
| Git | 2.25 或更高 | 用于克隆仓库及版本管理，首次安装必需 |
| YAML 解析库 (gopkg.in/yaml.v3) | 自动由 Go Modules 管理 | 用于读取配置文件，无需手动安装 |
| 系统内存 | 建议 512 MB 及以上 | 运行服务的最小内存要求，含健康探测缓存 |
| 可用磁盘空间 | 200 MB 及以上 | 用于存放二进制文件、日志及统计数据的本地存储 |
| 网络连通性 | 出站 80/443 端口开放 | 健康探测功能需要访问目标域名的 HTTP/HTTPS 服务 |
| 时区配置 | UTC+8 或本地时区 | 影响日志时间戳与统计看板的时间显示 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|------|----------|-----------|
| 用户指南 | `docs/user-guide/getting-started.md` | 如何首次运行、配置外链、查看导航页面与健康状态？ |
| 运维手册 | `docs/ops/deployment-options.md` | 支持哪些部署方式（二进制、Docker、K8s）？如何设置反向代理与 HTTPS？ |
| 开发参考 | `docs/dev/api-contract.md` | 内部模块间的接口定义是什么？如何扩展新的解析适配器或统计存储后端？ |
| 故障排查 | `docs/troubleshooting/common-issues.md` | 健康探测返回超时如何处理？配置文件加载失败如何定位？日志级别如何调整？ |
| 性能调优 | `docs/performance/scaling.md` | 当外链数量超过 500 个时，如何优化探测并发数与缓存策略？ |
| 安全说明 | `docs/security/access-control.md` | 如何配置 IP 白名单与令牌认证？统计信息是否包含敏感数据？ |

## 资源列表

本项目的核心价值在于对外部资源的有效组织。以下列表包含本项目示例配置及文档中引用的外部站点，所有 URL 均按原始格式原样收录。

数据服务类

- <code>tiqiuwang.org.cn</code>

- <code>lanqiubifen888.org.cn</code>

- <code>lanqiubifennbanba.org.cn</code>

- <code>qiutanzuqiubifen777.org.cn</code>

- <code>qiutanzuqiubifen500.org.cn</code>

- <code>500bifen365.org.cn</code>

- <code>500bifenwang365.org.cn</code>

## 项目结构

```
navigator-core/
├── cmd/
│   └── navigator-core/                # 主程序入口，含 main 函数与命令行参数解析
│       └── main.go
├── internal/
│   ├── adapter/                       # 解析适配器实现，定义不同域名的请求构建与响应解析
│   │   ├── http_client.go            # 带超时与重试的 HTTP 客户端封装
│   │   └── registry.go               # 适配器注册表，支持按域名动态选择解析策略
│   ├── health/                        # 健康探测模块，周期性检查外链可达性
│   │   ├── checker.go                # TCP/HTTP 探测核心逻辑
│   │   └── scheduler.go              # 基于 cron 表达式的探测调度器
│   ├── stats/                         # 访问统计与计数模块
│   │   ├── counter.go                # 基于内存的原子计数器，定期落盘至 JSON
│   │   └── geoip.go                  # 基于 IP 库的粗略地域解析（可选）
│   ├── config/                        # 配置加载与热更新模块
│   │   ├── loader.go                 # YAML 文件读取与反序列化
│   │   └── watcher.go                # 基于 fsnotify 的文件变更监听
│   └── web/                           # Web 服务与模板渲染
│       ├── server.go                 # HTTP 服务启动与路由注册
│       └── templates/                # 导航页面的 Go 模板文件
│           ├── index.tmpl
│           └── detail.tmpl
├── configs/
│   ├── example.yaml                   # 完整配置示例，含外链分组、探测间隔、访问控制参数
│   └── production.yaml.example        # 生产环境推荐配置模板
├── docs/                              # 完整文档目录（见文档导航章节）
├── scripts/
│   ├── build.sh                       # 跨平台编译脚本
│   └── migrate.sh                     # 配置版本迁移工具
├── test/
│   ├── integration/                   # 集成测试用例，模拟外部服务与配置变更
│   └── mock/                          # 模拟外部站点的 stub 服务器
├── Makefile                           # 构建与安装任务定义
├── go.mod                             # Go Modules 依赖管理文件
├── go.sum                             # 依赖校验和
└── README.md                          # 本文件
```

## 贡献指南

1. 查阅 `docs/dev/contributing.md` 了解完整的开发环境搭建流程与代码规范要求。所有新增功能或修复必须附带对应的单元测试用例，覆盖率不低于百分之八十。

2. 在 GitHub Issue 列表中查找标记为 `help-wanted` 或 `good-first-issue` 的待解决问题，并在评论中表明认领意图，避免多人重复工作。若计划引入新的适配器或外部依赖，请先通过 Issue 讨论其必要性与兼容性。

3. 提交代码前执行 `make lint` 与 `make test` 确保静态检查与测试通过。使用 `git commit --signoff` 签署开发者原创声明，提交信息需遵循约定式提交格式（如 `feat: 新增某某适配器`、`fix: 修复探测超时未重试问题`）。

4. 发起 Pull Request 时，需在描述中关联对应的 Issue 编号，并简要说明实现思路、测试覆盖情况以及配置变更影响。PR 至少需要一位项目维护者进行 Code Review，通过后方可合并。

5. 文档更新与翻译改进同样受到欢迎，请将文档修改与代码变更分开提交，便于评审。新增外链资源或示例配置时，需确保所有 URL 符合项目收录标准（不涉及非法内容且公开可访问）。

## 常见问题

**Q：健康探测功能是否会对外部站点造成较大的访问压力？**  
A：默认情况下，每个目标站点的探测间隔为 5 分钟，且仅执行一次简单的 TCP 连接或 HTTP HEAD 请求，不下载完整页面内容。探测并发数可通过 `health.concurrency` 参数限制（默认 10），对于流量敏感的外部站点，建议将探测间隔调至 30 分钟以上，或通过配置排除特定域名。

**Q：统计看板中记录的 IP 信息是否涉及用户隐私？**  
A：统计模块仅存储 IP 地址的前三段（如 192.168.1.x），不保留完整 IP，也不与任何用户身份标识进行关联。该设计旨在粗略分析访问地域分布，而非追踪个体行为。若仍需进一步加强隐私保护，可在配置中关闭 `stats.geoip_enabled` 选项，此时仅记录计数而不记录任何 IP 信息。

**Q：配置文件热加载生效后，正在进行的健康探测任务会中断吗？**  
A：不会。热加载机制仅影响配置读取模块，而健康探测调度器在启动时会将当前配置的快照载入内存。修改配置后，新配置会在下一次调度周期（即下一个探测间隔）开始时生效，正在执行中的探测任务不受影响。若需立即重置调度器，可发送 `SIGHUP` 信号给进程，该信号会触发配置全量重载并重置所有定时任务。

## 许可证

本项目采用 MIT 许可证。您可以在遵守许可证条款的前提下自由使用、修改、分发本软件，包括商业用途。详细版权与免责声明请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 7 | 生成时间: 2026-08-02 21:18:07
