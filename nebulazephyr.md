# BifenHub

BifenHub 是一个面向体育数据爱好者、竞彩分析人员与实时比分开发者的技术资源导航站。本项目不提供任何数据源或爬虫脚本，而是以人工筛选与社区投票机制，聚合全球范围内稳定、低延迟、高覆盖度的公开比分与赛事信息入口。项目目标用户为需要快速定位可靠比分接口的客户端开发者、数据分析师，以及希望减少搜索引擎噪音、直接获取可用外链的终端用户。通过本仓库，用户可在五分钟内完成环境配置，并获取一份可即时使用的结构化资源清单，从而将精力集中于数据处理与业务逻辑，而非链接可用性的反复验证。

## 功能概览

- **按赛事类型分类索引**：将资源按足球、篮球、综合体育等维度划分，每个类别下提供至少两个独立域名入口，避免单点失效。
- **可用性状态标记**：每个收录链接附带近七日的 HTTP 状态码与响应时间波动区间，辅助用户判断当前最佳接入节点。
- **静态页面镜像快照**：对每个资源主页生成每日一次的静态 HTML 摘要，便于在目标站点短暂不可用时仍能查阅其基础结构。
- **自定义别名系统**：允许用户为频繁使用的链接设置简短别名，并通过本地配置文件批量导入导出，简化日常调用。
- **变更日志订阅**：提供 Atom 格式的更新订阅源，当链接失效、域名迁移或 SSL 证书变更时，订阅端将在四小时内收到通知。
- **多语言筛选视图**：支持按页面语言（简体中文、繁体中文、英文）快速过滤资源，适配不同区域开发团队的阅读习惯。
- **性能基准测试工具**：内置基于 curl 的轻量级测速脚本，可对全部或部分资源执行并发延迟探测，并生成 CSV 格式报告。

## 应用场景

- **实时数据看板开发**：团队在构建赛事直播监控面板时，可通过本仓库的索引快速切换不同数据源，以对比数据更新频率与字段完整性，从而选择最适合自身业务的主备源。
- **竞彩模型回测**：分析师需要长期历史比分数据进行模型验证。本仓库提供的域名列表可作为数据采集调度的起点，配合定时任务实现多源冗余抓取，降低单源反爬策略导致的样本缺失风险。
- **运维故障应急切换**：当生产环境依赖的比分 API 出现地域性不可达时，运维人员可参考本仓库的资源状态表，在数分钟内手工将流量切换至备用域名，减少业务中断时长。
- **开源项目文档引用**：开发者在其体育类开源工具的 README 中，可直接引用本仓库作为“推荐数据源”章节的外部参考，避免自身维护庞大且易过时的链接列表。

## 快速开始

以下步骤适用于 Linux 与 macOS 环境，Windows 用户建议通过 WSL2 或 Git Bash 执行。

```bash
# 1. 克隆仓库至本地
git clone https://github.com/bifenhub/bifenhub-index.git
cd bifenhub-index

# 2. 安装依赖（需 Python 3.9+ 及 pip）
pip install -r requirements.txt

# 3. 执行本地索引构建与健康检查
python build_index.py --check-all
```

执行完毕后，`output/` 目录下将生成 `index.json` 与 `status_report.md`，前者为结构化资源列表，后者为可读性良好的状态摘要。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Python | 3.9 至 3.12 | 用于运行索引构建、测速与快照生成脚本 |
| curl | 7.68.0 或更高 | 用于实际执行 HTTP 探测，依赖系统内置工具 |
| git | 2.25.0 或更高 | 仓库克隆与版本管理 |
| make | 3.82 或更高 | 用于自动化任务编排（可选，但建议安装） |
| 网络带宽 | 下行不低于 1 Mbps | 保证快照下载与并发测速的稳定性 |
| 磁盘空间 | 至少 200 MB | 用于存储每日快照缓存及日志文件 |
| 操作系统 | Linux / macOS / WSL2 | 脚本未在原生 Windows 命令行下完整测试 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `docs/user-guide.md` | 如何添加自定义别名、如何订阅变更通知、如何导出筛选结果 |
| 运维参考 | `docs/operations.md` | 如何调整测速超时阈值、如何清理过期快照、如何迁移至私有化部署 |
| 开发者指南 | `docs/developer.md` | 索引 JSON Schema 定义、插件扩展接口、单元测试运行方法 |
| 常见调优 | `docs/tuning.md` | 对于高并发场景，如何优化测速并发数；对于低带宽环境，如何禁用快照功能 |
| 故障排查 | `docs/troubleshooting.md` | 常见 DNS 解析失败处理、SSL 证书兼容性说明、代理环境配置示例 |

## 资源列表

### 综合比分类

<code>bifenwang365.org.cn</code>

### 篮球专项类

<code>lanqiubifen365.org.cn</code>

### 足球即时类

<code>qiutanzuqiubifen888.org.cn</code>

<code>qiutanbifen888.org.cn</code>

<code>zuqiujishibifen365.org.cn</code>

### 五百比分专项类

<code>500bifen500.org.cn</code>

<code>500bifenwang500.org.cn</code>

## 项目结构

```
bifenhub-index/
├── build_index.py          # 主入口脚本，协调资源加载、健康检查与输出生成
├── requirements.txt        # Python 依赖清单（requests, pyyaml, lxml 等）
├── Makefile                # 封装常用任务（全量检查、增量更新、清理缓存）
├── config/
│   ├── sources.yaml        # 资源主列表（含 URL、类别、备注字段）
│   └── aliases.yaml        # 用户自定义别名映射表（默认空，由用户填充）
├── src/
│   ├── checker.py          # 封装 curl 调用，执行并发状态探测与响应时间解析
│   ├── snapshot.py         # 使用 requests 与 lxml 下载页面摘要并提取 title/meta
│   ├── feed.py             # 生成 Atom 订阅 XML 文件
│   └── utils.py            # 日志格式化、文件锁、重试装饰器等通用工具
├── output/                 # 构建结果输出目录（gitignore 保护，运行时生成）
│   ├── index.json          # 完整资源索引，含状态时间戳与延迟中位数
│   └── status_report.md    # 人类可读的状态摘要表格
├── cache/                  # 快照与历史状态缓存（按日期分目录，自动轮转清理）
│   └── 2026-08-02/         # 示例日期目录，内含各资源的 .html 摘要
├── tests/                  # 单元测试与集成测试脚本（使用 pytest 框架）
│   ├── test_checker.py
│   └── test_snapshot.py
└── docs/                   # 完整文档存放处（详见文档导航章节）
    ├── user-guide.md
    ├── operations.md
    ├── developer.md
    ├── tuning.md
    └── troubleshooting.md
```

## 贡献指南

1. 复刻本仓库至个人账户，并在本地创建功能分支，分支命名遵循 `feature/简述修改内容` 或 `fix/问题编号` 格式。
2. 在 `config/sources.yaml` 中按现有格式追加或修改资源条目，必须附带来源说明（官方页面或公开文档引用）以及至少一个备选端口或备用域名。
3. 执行 `make check` 确保新增资源的可用性测试通过，且现有索引未发生结构性破坏；若新增字段，需同步更新 `docs/developer.md` 中的 Schema 说明。
4. 提交变更时，使用约定式提交规范（如 `feat: add new basketball source` 或 `fix: correct timeout for existing domain`），并在 PR 描述中附上本地测试截屏或日志摘要。
5. 提交 Pull Request 后，等待维护者审核；若 CI 流程（GitHub Actions）中的测速步骤连续三次失败，则 PR 将被自动标记为需修改状态。

## 常见问题

**问：部分链接在本地无法访问，但状态报告显示可用，应如何处理？**

答：这通常由地域 DNS 解析差异或运营商网络策略导致。建议先更换公共 DNS（如 1.1.1.1 或 8.8.8.8）后重试。若仍不可达，可在 Issue 中附上 `curl -v` 输出和本地 IP 段信息，维护者将根据多地域探测结果调整该链接的可用性标记，并可能将其降级为“区域可用”类别。

**问：快照功能会存储敏感数据或用户信息吗？**

答：快照脚本仅下载目标页面的公开 HTML 内容，并提取 `<title>` 与 `<meta name="description">` 标签，不执行任何 JavaScript 或提交表单，因此不会触发 Cookie 或会话信息传递。存储的快照完全公开且可删除，用户可通过 `make clean-snapshots` 手动清除本地缓存。

**问：如何保持本地索引与上游仓库同步，同时保留个人别名配置？**

答：项目设计将别名文件 `config/aliases.yaml` 与主资源列表分离。执行 `git pull` 时，若 `sources.yaml` 发生变更，脚本会自动合并新资源至本地索引，而 `aliases.yaml` 不会被覆盖。建议用户定期执行 `make diff` 查看上游变更条目，并手动将新增资源的别名补充至个人配置中。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-02 21:18:07
