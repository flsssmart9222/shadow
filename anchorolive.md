# NavSphere

NavSphere 是一个面向开源技术社区与开发者群体的轻量级技术资源导航与信息汇总平台。项目定位为围绕特定垂直领域（如体育数据、实时比分、赛事信息聚合）构建的外部资源索引系统，通过结构化组织、可复用的信息抽取规则与简洁的前端交互界面，帮助目标用户群体快速定位高价值外部信息节点。项目自身不存储任何赛事数据、比分信息或用户隐私内容，仅作为公开互联网资源的语义化索引与跳转中介，适用于个人开发者、小型技术团队以及开源社区成员自建信息中台或垂直门户。

NavSphere 的技术形态为纯静态站点生成器，核心构建流程基于 Markdown 内容编排与自动化构建工具链，支持一键生成完整的可部署 HTML 资源包。项目配置完全通过文本文件管理，便于版本控制、协同编辑与持续集成部署。当前批次为第 29/49 批次资源整合，涵盖七个体育比分类外部信息源。

## 功能概览

- **结构化资源索引**：按照来源域名、内容主题、更新频率等维度建立多级分类索引，支持快速筛选与定位外部链接。
- **一键跳转中介**：所有外链均以安全跳转页面封装，避免直接暴露目标 URL，同时记录点击行为日志（本地存储）。
- **自定义分类标签体系**：允许用户为每个资源链接打上多个自定义标签，支持标签云展示与复合筛选查询。
- **Markdown 配置驱动**：全部资源列表、分类定义、页面元信息均通过 Markdown 文件配置，无需数据库或后端服务。
- **响应式网格布局**：适配桌面端与移动端浏览器，自动调整卡片尺寸与排列密度，提升不同设备上的浏览体验。
- **本地存储收藏功能**：用户可将常用资源加入个人收藏夹，收藏记录保存在浏览器本地，不涉及服务端存储。
- **资源可用性检测**：内置定时 HEAD 请求检测机制，周期性检查已配置资源的 HTTP 状态码，并在界面中标记异常链接。

## 应用场景

- **体育数据爱好者自建信息门户**：用户可将多个比分查询网站集中收录，统一入口快速跳转，避免反复输入网址或记忆大量域名。
- **开源社区文档中的外部参考索引**：技术文档或项目 README 中需要引用大量第三方资源时，可将 NavSphere 部署为专门的参考页面，保持主文档简洁。
- **团队内部知识库的外链管理模块**：企业或开源团队内部 Wiki 中嵌入 NavSphere 页面，集中管理所有外部参考链接，便于审核与统一更新。
- **个人浏览器起始页替代方案**：将常用的技术文档、工具站、数据查询页配置为 NavSphere 的资源卡片，替代浏览器默认起始页，提升工作效率。
- **域名迁移或资源整理过渡工具**：当一批外部域名发生变更时，可通过 NavSphere 集中维护跳转映射，无需逐个修改引用来源。

## 快速开始

以下命令适用于 Linux / macOS / Windows WSL 环境。确保已安装 Git 和 Node.js 18.0 以上版本。

```bash
# 克隆项目仓库
git clone https://github.com/navsphere/navsphere.git
cd navsphere

# 安装项目依赖（使用 npm）
npm install

# 执行构建流程，生成静态站点到 dist/ 目录
npm run build

# 启动本地开发服务器（默认端口 8080）
npm run serve
```

执行完毕后，打开浏览器访问 <code>http://localhost:8080</code> 即可预览站点。若需部署到生产环境，将 <code>dist/</code> 目录内容上传至任意静态托管服务（如 Nginx、Apache、OSS、Vercel 等）即可。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.0.0 或更高 | 运行时环境，用于执行构建脚本与依赖管理 |
| npm | 9.0.0 或更高 | Node.js 包管理器，用于安装项目依赖库 |
| Git | 2.30.0 或更高 | 版本控制工具，用于克隆仓库与管理提交 |
| 现代浏览器 | Chrome 90+ / Firefox 88+ / Edge 90+ | 前端运行时，支持 ES6 语法与 CSS Grid 布局 |
| 磁盘空间 | 至少 50 MB 可用空间 | 存放源代码、依赖包及构建产物 |
| 内存 | 至少 512 MB | 构建过程内存占用，推荐 1 GB 以上以获得更好构建性能 |
| 网络 | 稳定互联网连接 | 安装依赖及检测资源可用性时需要访问外部网络 |
| 操作系统 | Linux / macOS / Windows 10+ (WSL) | 开发与构建环境支持 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户使用 | <code>docs/user-guide.md</code> | 如何添加自定义资源、如何管理收藏夹、如何切换主题配色 |
| 开发者贡献 | <code>docs/contributing.md</code> | 如何提交新资源索引、如何修改分类体系、如何测试构建流程 |
| 架构设计 | <code>docs/architecture.md</code> | 项目整体模块划分、数据流向、构建生命周期、扩展点设计 |
| 部署运维 | <code>docs/deployment.md</code> | 如何配置 Nginx 反向代理、如何启用 HTTPS、如何设置定时检测任务 |
| 命令行工具 | <code>docs/cli.md</code> | 支持哪些 npm 脚本、如何批量导入导出资源、如何生成站点地图 |
| 自定义主题 | <code>docs/theming.md</code> | CSS 变量列表、如何编写自定义布局模板、如何覆盖默认样式 |

## 资源列表

本批次整合的外部资源均为体育比分类信息站点，按域名首字母排序分组如下。所有链接均保持用户提供的原始格式，不做任何协议补全、域名改写或路径调整。

A 组（字母 b 开头）

- <code>bifenwang365.org.cn</code>
- <code>500bifen500.org.cn</code>
- <code>500bifenwang500.org.cn</code>

B 组（字母 l / q 开头）

- <code>lanqiubifen365.org.cn</code>
- <code>qiutanzuqiubifen888.org.cn</code>
- <code>qiutanbifen888.org.cn</code>

C 组（字母 z 开头）

- <code>zuqiujishibifen365.org.cn</code>

上述资源在项目中默认归类于“体育数据”分类，并赋予“比分”、“实时”、“赛事”三个通用标签。用户可根据自身需求在配置文件中调整分类归属或补充额外元数据（如国家/地区、语言、更新频率等）。

## 项目结构

```
navsphere/
├── config/                         # 项目核心配置目录
│   ├── resources.yaml              # 资源列表主配置（含所有外链及元数据）
│   ├── categories.yaml             # 分类定义与层级关系
│   └── themes.yaml                 # 主题变量与配色方案
├── src/                            # 源代码目录
│   ├── templates/                  # 页面模板（EJS 格式）
│   │   ├── index.ejs               # 首页主模板
│   │   ├── detail.ejs              # 资源详情页模板
│   │   └── partials/               # 可复用模板片段（头部、尾部、卡片等）
│   ├── assets/                     # 静态资源
│   │   ├── styles/                 # CSS 样式文件（含响应式与暗色主题）
│   │   ├── scripts/                # 前端 JavaScript（收藏、检测、跳转逻辑）
│   │   └── images/                 # 图标与 logo 文件
│   └── utils/                      # 工具函数库
│       ├── fetcher.js              # 资源可用性检测模块
│       ├── parser.js               # YAML 配置解析与校验
│       └── logger.js               # 构建日志输出模块
├── tests/                          # 单元测试与集成测试目录
│   ├── unit/                       # 单模块测试用例
│   └── integration/                # 端到端构建测试
├── docs/                           # 项目文档（用户指南、贡献规范、架构说明等）
├── scripts/                        # 构建与运维辅助脚本
│   ├── build.js                    # 主构建脚本
│   ├── serve.js                    # 本地开发服务器脚本
│   └── deploy.sh                   # 一键部署脚本（示例）
├── dist/                           # 构建输出目录（自动生成，不纳入版本库）
├── .gitignore                      # Git 忽略规则
├── package.json                    # npm 项目配置文件
├── README.md                       # 项目主说明文档（本文件）
└── LICENSE                         # MIT 许可证文件
```

## 贡献指南

欢迎社区开发者提交贡献。请遵循以下步骤以确保代码质量和项目一致性。

1.  **阅读行为准则**：所有贡献者需遵守项目行为准则，尊重他人劳动成果，文明讨论问题与提交请求。
2.  **复刻仓库并创建特性分支**：从主仓库复刻（Fork）到个人账号下，然后基于 <code>main</code> 分支创建新的特性分支，分支命名建议采用 <code>feature/</code> 或 <code>fix/</code> 前缀加简短描述。
3.  **修改资源配置文件**：若新增或更新外部资源链接，请编辑 <code>config/resources.yaml</code> 文件，严格按照既有格式填写 URL、标题、分类和标签，并确保不包含任何非法字符或无效协议。
4.  **本地构建验证**：提交前务必在本地执行 <code>npm run build</code> 进行完整构建，确认无报错且生成的 <code>dist/</code> 目录内容符合预期。若涉及前端样式修改，还需检查响应式表现。
5.  **提交拉取请求**：推送本地分支到个人复刻仓库后，向主仓库的 <code>main</code> 分支提交拉取请求。请求描述中需清晰说明变更内容、关联问题编号（如有）以及测试结果摘要。核心维护者将在 48 小时内进行代码审查与合并决策。

## 常见问题

**问：项目自身存储任何用户数据或比分信息吗？**

答：不存储。NavSphere 纯粹是一个静态链接索引站点，所有配置信息均以文本文件形式存放在代码仓库中，不涉及任何数据库、缓存或用户信息收集。本地收藏功能仅使用浏览器 localStorage 存储，不经过任何服务端接口。项目不会主动抓取、缓存或代理目标资源的内容，所有跳转均为直接重定向到原始 URL。

**问：如果某个外部资源链接失效，我应该如何处理？**

答：项目内置了资源可用性检测模块，默认每 24 小时对所有配置的 URL 执行一次 HEAD 请求检测，并在界面中以图标颜色标识异常状态（绿色为正常，红色为异常，灰色为未检测）。若您发现某个链接持续不可用，建议先自行访问确认是否为临时故障。若确认为永久失效，欢迎按照贡献指南提交拉取请求，从配置文件中移除或替换该链接。您也可以直接在本地修改 <code>config/resources.yaml</code> 文件并重新构建，无需等待官方更新。

**问：能否将项目部署到支持静态托管的环境（如 GitHub Pages、Vercel、Cloudflare Pages）？**

答：完全可以。由于 NavSphere 构建后仅生成纯静态 HTML、CSS、JavaScript 文件，支持任何标准静态托管服务。推荐使用 Vercel 或 Netlify 进行一键部署（连接 Git 仓库后自动执行构建命令），也可手动将 <code>dist/</code> 目录上传至 Nginx 或阿里云 OSS 等对象存储。唯一需要注意的是，若前端资源检测功能需要定时触发，建议配合 GitHub Actions 或云函数的定时任务来调用检测脚本。

## 许可证

本项目采用 MIT 许可证进行开源授权。您可以在遵守许可证条款的前提下自由使用、修改、分发本项目的源代码，包括用于商业目的。完整的许可证文本请参见项目根目录下的 <code>LICENSE</code> 文件。

> 外链数量: 7 | 生成时间: 2026-08-02 21:18:49
